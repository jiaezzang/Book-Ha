<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bookha.main.mapper.MapperNotice">
    <select id="list" resultType="com.bookha.main.dto.DTONoticeBoard">
        SELECT
        	no_seq,
        	no_user_num,
            no_subject,
            no_hit,
            no_date
        FROM
        	Notice_board
    </select>
    
    <select id="view" parameterType="int" resultType="com.bookha.main.dto.DTONoticeBoard">
        SELECT
        	no_seq,
        	no_user_num,
            no_subject,
            no_content,
            no_hit,
            no_date
        FROM
        	Notice_board
        WHERE
        	no_seq = #{seq}
    </select>
    
     <update id="viewHit" parameterType="int">
    	UPDATE
    		Notice_board
    	SET
    		no_hit = no_hit + 1
    	WHERE
    		no_seq = #{seq}
    </update>
    
    <insert id="writeOk" parameterType="com.bookha.main.dto.DTONoticeBoard">
        INSERT INTO Notice_board (
            no_seq,
            no_user_num,
            no_subject,
            no_content,
            no_hit,
            no_date
        ) VALUES (
			0,
			#{user_num},
			#{subject},
			#{content},
			0,
			now()
		);
    </insert>
    
    <select id="modify" parameterType="int" resultType="com.bookha.main.dto.DTONoticeBoard">
        SELECT
        	no_seq,
            no_user_num,
            no_subject,
            no_content,
            no_hit,
            no_date
        FROM
        	Notice_board
        WHERE
        	no_seq = #{seq}
        AND
        	no_user_num = #{user_num}
    </select>
    
    <update id="modifyOk" parameterType="com.bookha.main.dto.DTONoticeBoard">
    	UPDATE
    		Notice_board
    	SET
    		no_subject = #{subject},
    		no_content = #{content}
    	WHERE
    		no_seq = #{seq}
    	AND
    		no_uwer_num = #{no_num}
    </update>
    
    <delete id="deleteOk" parameterType="com.bookha.main.dto.DTONoticeBoard">
    	DELETE
    	FROM
    		Notice_board
    	WHERE
    		no_seq = #{seq}
    	AND
    		no_user_num = #{no_num}
    </delete>
    
</mapper>